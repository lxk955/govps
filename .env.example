# 前端环境变量（构建/运行时均可读取）
# /api/* 与 /go/* rewrite 的转发目标（同域转发，见 next.config.ts）
# 默认 http://localhost:8000；生产指向 FastAPI 服务地址，未来拆分 api.govps.xyz 时仅改此值
# API_ORIGIN=http://localhost:8000

# 浏览器直连后端的地址（IP 检测专用，见 src/lib/api/endpoints.ts checkIp）。
# 留空则回退走 /api rewrite——本地开发够用；线上必须配置，否则 IP 检测
# 会返回前端服务器的出口 IP 而不是访客自己的公网 IP。
# NEXT_PUBLIC_API_URL=http://localhost:8000
